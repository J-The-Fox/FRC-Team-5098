package frc.components;

/*
 * This is an interface. If a class implements any interface, it must have all of the methods in that interface.
 * The methods defined in an interface are abstract, meaning they have no body.
 */
public interface IComponent {
    
    public void update();
}
